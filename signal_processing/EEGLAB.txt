pop_export.m
    -change it to always use comma separation instead of a tab

attempt to make the use automatic.

find a way to generate .set files`



interesting functions:

pop_loadset() for loading existing set
pop_eegfiltnew()  to filter data
