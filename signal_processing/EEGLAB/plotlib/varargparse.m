%% return struct with vararg variables

%%todo
% write the function to not just be finputcheck

function result = varargparse( varargin, fieldlist )
    result = finputcheck(varargin, fieldlist);

end